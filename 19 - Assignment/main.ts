let GuestList = ["Sami", "Shahbaz", "Naveed", "Hammad"];

// GuestList.forEach(OneGuest =>console.log(`Salam ${OneGuest},\n would you like to dinner with me?`));

// Guestlist.length method use to count guest.
let lengthGuest: number = GuestList.length;

console.log(`We are Inviting the total ${lengthGuest} guests. `);
